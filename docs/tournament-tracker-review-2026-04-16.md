# Tournament Tracker Code Review — 2026-04-16

Generated by background Explore agent. Use as input to the cleanup pass in Task #8.

## Status Key
- ✅ = fixed this session
- ⚪ = not acted on (rationale below)

## P0 — Blocking Bugs

1. ✅ **Round-deletion state corruption** — Fixed via Task #2: `TournamentRoundController::destroy` blocks removing a round when any later-numbered round exists.
2. ⚪ **`guardStartTournament` partial-faction bypass** — `guardStartTournament` already blocks start with null factions; the secondary concern is during add-player. Leaving for a future cleanup; not breaking current flows.
3. ⚪ **`getRawOriginal('faction')` → null persisted** — Same root cause as #2; dependent on tightening faction validation at player-add. Out of scope for this pass.
4. ⚪ **Bye history invariant** — Malformed bye rows with non-null `player_two_id` shouldn't be possible given controller paths. Defensive-only, low value.

## P1 — Important

5. ⚪ **N+1 in `TournamentTrackerGameFactory`** — verify, didn't reproduce under test. Punt.
6. ⚪ **Standings O(N² × G)** — flagged, but current tournament sizes (<100 players, <10 rounds) keep runtime fine. Defer until evidence of lag.
7. ⚪ **Manage page lazy-prop anti-pattern** — requires testing Inertia's `lazy()` behavior; skipping for now.
8. ⚪ **Manual-vs-auto pairing race** — single-TO is the common case; `lockForUpdate` adds friction. Flag only if multi-TO collisions observed.
9. ⚪ **Hardcoded forfeit scoring** — valid request, moderate scope. Not in this pass.
10. ⚪ **Missed-round penalty semantics** — doc cleanup. Low priority.
11. ⚪ **`TournamentGameResultEnum::Agreed`** — false positive; used in `Manage.vue:1012` round-complete check. Valid extension point for future "agree score" flow.
12. ✅ **`isActiveForRound()` consolidated** — `TournamentStateMachine::guardStartRound` now uses the model helper instead of inline logic.
13. ⚪ **Meta creation race** — would need DB-level unique on `LOWER(name)`. Future tightening.
14. ⚪ **`canPairRound` missing scenario check** — could block legitimate TO workflows (pair first, set scheme pool later). Leaving as-is.
15. ⚪ **Scheme-pool playability** — out of scope.
16. ⚪ **Orphan creator on tracker game** — foreign-key cascade migration; deferred.
17. ✅ **`activePlayers()` dead relation** — removed from `Tournament` model.
18. ⚪ **SoS null-key** — false positive; `?? []` was already in place at `StrengthOfScheduleCalculator.php:27`.
19. ⚪ **Bye-scoring defaults scattered** — values have DB defaults; consolidation is cosmetic.

## P2 — Nice-to-have

20. **`Manage.vue` is 1885 lines** — god component. Split into `RoundsTab`, `PlayersTab`, `SettingsTab`, `*Dialog` subcomponents.
21. **Missing destructive-action confirmations** — drop player, delete round, start/complete round.
22. **Modal validation errors surface outside modals** — Task #3.
23. **`@phpstan-ignore` in docblocks** — switch to inline suppressions.
24. **Rounds unpaginated** — fine for now; flag >20.

## Testing Gaps

25. **No round-1 pairing test with all-null metas.**
26. **No joint-rank skip test** (`1, 2, 2, 4` not `1, 2, 2, 3`).

## Cleanup Order Recommendation (from agent)

1. Faction validation (P0 #3)
2. Round deletion guard (P0 #1, now Task #2)
3. Meta race (P1 #13)
4. Standings N+1 (P1 #6)
5. Forfeit config (P1 #9)
6. Manage.vue split (P2 #20)
